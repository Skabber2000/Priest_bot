{
  "name": "priest-worker",
  "version": "2.0.0",
  "private": true,
  "description": "Cloudflare Worker proxy between the Priest_bot web app and the Anthropic Claude API.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5",
    "wrangler": "^3.80.0"
  }
}
